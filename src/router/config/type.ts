export const CONFIG_ACCOUNT_PATH = {
  BASE: '/config/account/',
  VIEW: (id: string | number = ':id') => `/config/account/view/${id}`,
};

export const CONFIG_PLAYER_PATH = {
  BASE: '/config/player/',
};

export const CONFIG_TIER_PATH = {
  BASE: '/config/tier/',
};

export const CONFIG_PROFILE_PATH = {
  BASE: '/config/profile/',
};
